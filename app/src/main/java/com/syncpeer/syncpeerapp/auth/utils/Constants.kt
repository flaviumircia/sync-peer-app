package com.syncpeer.syncpeerapp.auth.utils

import java.util.regex.Pattern

object Constants {
    const val JWT_FILE_NAME = "jwts"
    const val SHARED_PREFERENCES_JWT_NAME = "jwt"
    const val EMAIL_REGEX = "^[A-Z0-9._%+-]+@[A-Z0-9.-]+\\.[A-Z]{2,6}\$"

}
